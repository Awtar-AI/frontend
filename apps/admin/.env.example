# Awtar Admin — Environment Variables
# Copy this file to .env.local and fill in your values.
# Never commit .env.local — it's in .gitignore.

# Backend API base URL (Nest app)
NEXT_PUBLIC_API_URL=http://localhost:3001
